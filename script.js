function Add(a, b) {
    return a + b;
}

function Subreact(c, d) {
    let subtractionValue = c - d;
    console.log(`The result of the subtraction opertation on both of the numbers gotten are ${subtractionValue}`);
    return subtractionValue;
}
